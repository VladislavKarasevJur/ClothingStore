<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.demo.clothingstore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ClotheInfo"></activity>
        <activity android:name=".exam" />
        <activity android:name=".hudiRpndpInfo" />
        <activity android:name=".switshot" />
        <activity android:name=".futbolka" />
        <activity android:name=".krosi" />
        <activity android:name=".hoodi" />
        <activity android:name=".catal" />
        <activity android:name=".info" />
        <activity android:name=".catalog" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>